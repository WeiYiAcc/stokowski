__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
build/
log/
*.log
.env
WORKFLOW.md
docs/plans/

# Stokowski runtime state (workspaces, results, logs, tasks)
# All daemon-managed; populated at runtime, never committed.
/agent-state/

# Workflow config (operator-local — copy from workflow.<mode>.yaml templates)
/workflow.yaml
/workflow.yml

# Prompt files (operator-local, examples and tournament are tracked)
prompts/*.md
!prompts/*.example.md
!prompts/*.tournament.example.md
